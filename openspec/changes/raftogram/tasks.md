## 1. Каркас репозитория и сервера

- [x] 1.1 Инициализировать Go-модуль и базовую структуру `cmd/raftogram/main.go` (тонкая сборка) и `internal/` пакетов
- [x] 1.2 Добавить конфигурацию кластера (node id, Raft bind, gRPC bind, список пиров) с валидацией нечётного кворума
- [x] 1.3 Подключить zap-логгер и флаги/ENV для конфигурации

## 2. Интеграция Raft-кластера

- [x] 2.1 Подключить `github.com/hashicorp/raft` и `github.com/hashicorp/raft-boltdb`: `LogStore`/`StableStore` на BoltDB, `SnapshotStore` через `raft.NewFileSnapshotStore` на локальном диске
- [x] 2.2 Реализовать сетевой транспорт между узлами для Hashicorp Raft и bootstrap/join по статической конфигурации
- [x] 2.3 Экспортировать health/metrics: роль узла, известный лидер, готовность к приёму клиентских мутаций
- [x] 2.5 Настроить пороги снапшотирования Raft (число записей и/или размер лога) и связку `Snapshot`/`Restore` с FSM

- [x] 3.1 Описать команды домена в `.proto` (Protobuf), wire-формат в логе Raft и версионирование записей лога
- [x] 3.2 Реализовать детерминированное применение команд к in-memory модели каналов/сообщений с лимитом размера сообщения и дедупликацией по `(channel_id, client_message_id)` при непустом ключе
- [x] 3.3 Реализовать снапшот/restore FSM, согласованный со снапшотами Raft
- [x] 3.4 Юнит-тесты: одинаковый порядок команд даёт идентичное состояние; отказ для oversized payload; два commit с одним `client_message_id` дают одно видимое сообщение

## 4. Клиентский gRPC API

- [x] 4.1 Описать protobuf для create channel, send, read history, subscribe stream и ошибки not-leader с подсказкой лидера
- [x] 4.2 Реализовать gRPC-сервис на лидере: мутации через `Apply`; на фолловере — корректные ошибки маршрутизации
- [x] 4.3 Реализовать read history с курсором/лимитом на согласованном состоянии лидера
- [x] 4.4 Реализовать subscribe-поток с документированным backpressure (буфер/flow-control gRPC)
- [x] 4.5 HTTP `/health` (или аналог) для liveness и полей роли Raft

## 5. Наблюдаемость и готовность к выкату

- [ ] 5.1 Структурированные логи ключевых переходов (election, snapshot, apply errors) без дублирования error log+return
- [ ] 5.2 Минимальная документация запуска: пример конфигурации трёх узлов и порядок старта
